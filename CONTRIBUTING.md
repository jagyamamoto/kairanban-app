# 参加のしかた

## 報告だけでも助かります

「導入してみたらここで詰まった」「高齢の会員がここで止まった」という報告は、
コードの修正と同じくらい価値があります。Issue にお書きください。

## Pull Request

1. まず Issue で「こうしたい」を共有してください（大きな変更ほど早めに）
2. `npm run check` と `npm run build` が通ることを確認してください
3. `bash tools/scrub-check.sh` を実行し、**地域固有の情報が混ざっていない**ことを確認してください

## 議論を歓迎しているところ

**ログイン方式（電話番号だけ）** は、利便性とリスクのトレードオフとして選んだものです。
「解決済み」ではありません。より良い方法の提案を募集しています。
背景は [docs/ログイン方式について.md](docs/ログイン方式について.md) をご覧ください。

## この設計を変えないでほしいところ

実機で高齢の方に試してもらって決めた部分です。変更する場合は理由を添えてください。

- **「ホーム画面に追加」の案内の帯は、画面の上から42%以内に収める**
  iPhoneの共有画面の上端が42.7%（実測）。超えると手順が隠れます。
  文字の大きさは `clamp(..., vh, ...)` で画面の高さに追従させています。固定pxに戻さないでください。
- **手順を時間で自動的に進めない**
  初版はそうしていましたが、実機テストで「案内」と「実際の画面」が食い違って失敗しました。
- **指で押すものは高さ44px以上**
- **状態を色だけで表さない**（○△●などの記号を併記する）
- **写真・ブログを一般公開できるようにしない**
- **`public/` に検証用のHTMLを置かない**（そのまま公開されます）

## 文章の書き方

利用者に見える文章は、**高齢の方が読んで分かること**を最優先にしています。

- 使わない言葉: PWA / インストール / 共有シート / スクロール / 簡単 / ステップ
- 画面に出ている文字は、鍵括弧つきで一字一句そのまま書く
- 動詞は「押す」に統一する
- 「かんたん」と書かない（できなかった人を傷つけます）
